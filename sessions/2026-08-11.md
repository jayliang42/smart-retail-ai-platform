# Session — 2026-08-11

## Outcome

Retail Operations API v0.1 is running with FastAPI, an in-memory test repository, PostgreSQL persistence, Docker Compose, and automated quality gates.

## What was implemented

- Store and SKU creation
- Inventory lookup
- Non-negative inventory adjustments
- Idempotent retry behavior using `request_id`
- PostgreSQL tables and transaction-based repository
- API, domain, repository, and PostgreSQL integration tests
- Dockerized API and PostgreSQL services

## Verification evidence

- `pytest`: 15 passed, 1 PostgreSQL test skipped without its opt-in URL
- PostgreSQL integration test: 1 passed
- Ruff: passed
- basedpyright: 0 errors, 0 warnings
- Docker Compose: API running; PostgreSQL healthy
- End-to-end replay: quantity remained 12 and the database contained one adjustment record

## Engineering continuation: versioned schema migrations

- Replaced runtime `metadata.create_all()` with Alembic.
- Added baseline migration `20260811_0001`.
- Added a one-shot Docker migration service that gates API startup.
- Upgraded the existing v0.1 database without losing its test inventory.
- Migrated a separate empty PostgreSQL database and confirmed all five expected tables.
- Confirmed the SQLAlchemy metadata matches the migration with `alembic check`.
- Confirmed PostgreSQL rejects a direct negative-inventory insert.
- Removed the temporary migration-test database after verification.

## Engineering continuation: device-event vertical slice

- Added typed device registration with constrained device types and statuses.
- Added idempotent device events with `observed_at`, `received_at`, and JSONB payloads.
- Added device create/get and event create/list FastAPI endpoints.
- Added Alembic migration `20260811_0002` for `devices` and `device_events`.
- Increased the local suite to 24 passing tests; both PostgreSQL integration tests pass.
- Verified an HTTP event replay produces exactly one PostgreSQL row.
- Verified the full `0001 → 0002` migration chain against a new temporary database, then removed it.
- Fixed local Alembic source-path resolution so checks do not depend on editable-install behavior.

Before moving to analytics, explain why device events keep two timestamps and why an arbitrary JSONB payload cannot yet be trusted as training or forecasting data.

## Engineering continuation: Inventory Intelligence baseline

- Added a versioned daily sales/inventory evaluation dataset with 30 labeled cases.
- Added strict canonical parsing and a data-quality report for invalid and duplicate rows.
- Added a stockout-only baseline and a richer explainable inventory-risk rules detector.
- Ensured rolling demand uses only earlier observations for each store/SKU.
- Added reproducible accuracy, precision, recall, and F1 evaluation through a CLI.
- Verified stockout-only F1 `0.4286` and risk-rules F1 `1.0000` on the synthetic v1 dataset.
- Recorded that the perfect result is only a contract/regression sanity check because the hand labels closely follow the same rule rubric.

Before the forecasting slice, explain in your own words:

1. Why can a detector have precision `1.0` and still be operationally weak?
2. Why must trailing demand for a date use only earlier dates?
3. Why does F1 `1.0` on this synthetic dataset not prove production performance?

## Engineering continuation: demand forecasting baseline

- Added last-value and trailing-mean one-step demand forecasters.
- Used the same three-observation warm-up for a fair 24-case comparison.
- Generated each forecast before appending the target day's observed demand.
- Recorded MAE, RMSE, WAPE, and mean bias through the same evaluation CLI.
- Measured last-value MAE `4.7500` versus trailing-mean MAE `3.2938` on v1.

Explain why random train/test splitting would be misleading for this time-ordered problem, and why
WAPE is more usable than MAPE when a store/SKU day can have zero sales.

## Engineering continuation: persisted analytics batch and API

- Added immutable analytics run metadata with dataset and configuration lineage.
- Added Alembic migration `20260811_0003` for runs, flagged anomalies, and forecasts.
- Added an executable batch path that validates all rows before persisting results.
- Added bounded run/anomaly/forecast GET endpoints with store and SKU filters.
- Rebuilt Docker, applied `0003`, and persisted `e2e-analytics-v1` to PostgreSQL.
- Queried run metadata, milk anomalies, and cereal forecasts through the live HTTP API.
- Increased verification to 36 local tests and 3 PostgreSQL integration tests.
- Migrated a fresh temporary database through all three revisions and confirmed 10 tables.

Explain why results are keyed by an immutable `run_id` instead of overwriting a single “latest”
table, and why the validation gate runs before persistence.

## Learner task — 60–90 minutes

First, run the API and inspect `http://localhost:8000/docs`. Trace this request through the code:

```text
POST /inventory/adjustments
→ Pydantic request schema
→ domain model and invariant
→ repository protocol
→ PostgreSQL transaction
→ response schema
```

Then implement `GET /stores/{store_id}` across the repository protocol, both repository implementations, the FastAPI route, and tests.

## Completion criteria

- Existing tests still pass.
- A known store returns HTTP 200 with its ID and name.
- An unknown store returns HTTP 404.
- At least one repository test and one API test cover the behavior.
- Ruff and basedpyright pass.
- Explain in your own words why `request_id` is required and why negative inventory returns 409 rather than 422.

Ask for a hint only after recording what you tried and the exact failing output.

## Engineering continuation: grounded Store Operations Copilot

- Added versioned SOP, refrigeration manual, and incident playbook ingestion.
- Added deterministic chunk IDs, stable citations, PostgreSQL pgvector storage, and HNSW search.
- Added structured cited answers and application-side citation allowlist validation.
- Added strict inventory, device, price, and work-order tools.
- Added a bounded multi-step Agent using OpenAI Responses function calls and structured output.
- Added persistent approval requests for inventory and price writes.
- Bound approval identity to API-key configuration and enforced manager/pricing-lead roles.
- Added timeout, bounded retry, maximum-step, structured error, and transient fallback paths.

## Engineering continuation: production hardening

- Added `copilot_eval_v1` with 60 deterministic cases.
- Measured retrieval Recall@3 `1.0000`, MRR `0.9778`, and `1.0000` accuracy for citation, tool, and approval-policy cases.
- Added request IDs, JSON latency logs, Agent token/tool/attempt telemetry, and configurable cost estimates.
- Added a bounded embedding LRU cache and transient provider fallback.
- Added GitHub Actions for static checks, migrations, unit/integration tests, Copilot regression evaluation, and image build.
- Added a Render blueprint with managed PostgreSQL, pre-deploy migrations, knowledge ingestion, health checks, and secret prompts.
- Rebuilt the Docker image as non-root `appuser` and verified the live PostgreSQL-backed health endpoint.
- Migrated a fresh database through `0001 → 0006`, confirmed 16 tables and the vector extension, then removed it.

No live OpenAI result was recorded because the current environment has no API key. No cloud resource
was created because an external account was not authorized.

## Engineering continuation: correlated tracing and concurrent-write hardening

- Propagated the HTTP request ID through Agent model attempts, tool results, and completion events.
- Added structured retry, provider-unavailable, and Agent-step-limit trace events without logging
  tool arguments or credentials.
- Verified HTTP, tool, and Agent events share the same request ID through an API-level test.
- Added transaction-scoped PostgreSQL advisory locks before inventory and price idempotency checks.
- Verified different concurrent first adjustments preserve both deltas, concurrent replay applies
  one request once, and concurrent first price changes both finish without unique-key races.
- Expanded verification to 72 local tests and 8 PostgreSQL integration tests.

## Engineering continuation: live-model evaluation readiness

- Added 12 end-to-end Agent cases for reads, errors, a low-risk write, two approval-gated writes,
  and four grounded manual/SOP questions.
- Added a CLI that records tool/status sequence accuracy, approval accuracy, required-section
  accuracy, latency, tokens, and optional cost.
- Verified evaluator mechanics with a scripted model and verified missing API credentials fail
  explicitly. The live suite remains unrun and has no reported score.

After completing the reserved `GET /stores/{store_id}` task, explain these three design decisions:

1. Why is checking model citations after generation stronger than only telling the model to cite?
2. Why does a high-risk tool return `approval_required` instead of waiting inside the Agent loop for a human?
3. Why is a perfect score on `copilot_eval_v1` still insufficient evidence for production LLM quality?

## Engineering continuation: authenticated direct writes and actor auditing

- Protected store, SKU, device, device-event, and inventory POST routes with server-resolved API-key identity and explicit role policy.
- Added an admin-only, bounded audit-event query with actor and resource filters.
- Persisted each first successful direct write and its actor audit event in one repository transaction; idempotent replays do not create duplicate audit events.
- Added Alembic migration `20260811_0007` and verified a fresh `0001 → 0007` upgrade creates 17 tables with pgvector enabled.
- Expanded verification to 75 local tests and 9 PostgreSQL integration tests; Ruff, basedpyright, and `alembic check` pass.
- Rebuilt Docker with a one-time environment-provided admin key: an unauthenticated store write returned 401, the authenticated write returned 201, and both `GET /audit-events` and PostgreSQL showed the same actor/resource event.
